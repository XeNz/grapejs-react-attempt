# GrapesJS + React

Attempt to try using [GrapesJS](https://grapesjs.com/) with react.

## Available Scripts

In the project directory, you can run:

### `yarn start`
### `yarn test`
### `yarn build`
